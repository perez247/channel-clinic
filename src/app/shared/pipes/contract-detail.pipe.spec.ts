import { ContractDetailPipe } from './contract-detail.pipe';

describe('ContractDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
